FROM ruby:3.2.0

# Default directory
ENV INSTALL_PATH /workspaces/hackdays-2023
RUN mkdir -p $INSTALL_PATH
WORKDIR /workspaces/hackdays-2023

# Install rails
RUN gem install rails

# Run a shell
# CMD ["/bin/bash"]
